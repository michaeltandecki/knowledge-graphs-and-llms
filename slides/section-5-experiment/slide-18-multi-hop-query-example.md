## Multi-Hop Query Example

Example question:

Which founders are connected to companies acquired by Google?

Conceptual traversal:

Google -> acquired -> YouTube -> founded_by -> Chad Hurley

**This query is trivial for a graph database but awkward for vector retrieval.**
